<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <form action="/products/<%=product._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="product[title]" value="<%=product.title%>">
        </div>
        <div>
            <label for="expiryDate">Expiry Date</label>
            <input type="date" id="expiryDate" name="product[expiryDate]" value="<%=product.expiryDate%>">
        </div>
        <div>
            <label for="description">Description</label>
            <textarea name="product[description]" id="description" cols="30" rows="10"><%=product.description%></textarea>
        </div>
        <button>Update Product</button>
        <a href="/products/<%= product._id%>">Back To Product</a>
    </form>
</body>
</html>